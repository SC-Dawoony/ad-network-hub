# Ad Network Management Streamlit 앱 설계 (다중 네트워크 지원)

## 1. 전체 구조 (확장 가능한 멀티 네트워크 아키텍처)

```
┌─────────────────────────────────────────────────────────────┐
│              Ad Network Management Hub                       │
│                (Streamlit Multi-Page App)                    │
│                                                               │
│  [Network Selector: BigOAds ▼ Fyber | InMobi | IronSource] │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
   ┌────▼────┐          ┌────▼────┐          ┌────▼────┐
   │  Create │          │  Create │          │  View   │
   │  Media  │          │   Slot  │          │  Lists  │
   │  (App)  │          │  (Unit) │          │         │
   └─────────┘          └─────────┘          └─────────┘
        │                     │                     │
        ▼                     ▼                     ▼
   [네트워크별         [네트워크별         [네트워크별
    필드 렌더링]        필드 렌더링]        데이터 표시]
```

### 네트워크별 지원 현황
| Network    | Get Apps | Create App | Get Units | Create Unit | Status |
|------------|----------|------------|-----------|-------------|--------|
| BigOAds    | ✅       | 🚧 구현예정  | ✅        | 🚧 구현예정   | Phase 1|
| Fyber      | ✅       | ❓         | ✅        | ❓          | Phase 2|
| InMobi     | ✅       | ❓         | ✅        | ❓          | Phase 2|
| IronSource | ✅       | ❓         | ✅        | ❓          | Phase 2|
| Mintegral  | ✅       | ❓         | ✅        | ❓          | Phase 3|
| Pangle     | ✅       | ❓         | ✅        | ❓          | Phase 3|
| Liftoff    | ✅       | ❓         | ✅        | ❓          | Phase 3|

## 2. 페이지별 와이어프레임

### 2.1 메인 페이지 (Home) - 네트워크 허브
```
┌──────────────────────────────────────────────────────────┐
│  🌐 Ad Network Management Hub                            │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  🔌 Network Selection                                     │
│  ┌────────────────────────────────────────────┐         │
│  │ Active Network: [BigOAds ▼]                │         │
│  │   Options: BigOAds, Fyber, InMobi,         │         │
│  │            IronSource, Mintegral, Pangle   │         │
│  └────────────────────────────────────────────┘         │
│                                                           │
│  📊 Connection Status                                     │
│  ┌────────────────────────────────────────────┐         │
│  │ Network    Status      Credentials         │         │
│  ├────────────────────────────────────────────┤         │
│  │ BigOAds    ✅ Active   Dev ID: 200...      │         │
│  │ Fyber      ⚠️ Not Set   -                  │         │
│  │ InMobi     ⚠️ Not Set   -                  │         │
│  │ IronSource ❌ Error     Invalid Token      │         │
│  └────────────────────────────────────────────┘         │
│                                                           │
│  📋 Quick Actions (for selected network)                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │ Create App  │  │ Create Unit │  │  View Lists │     │
│  │  (Media)    │  │   (Slot)    │  │             │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
│                                                           │
│  📈 Multi-Network Statistics                              │
│  ┌────────────────────────────────────────────┐         │
│  │ Network    Apps    Units    Last Sync      │         │
│  ├────────────────────────────────────────────┤         │
│  │ BigOAds     12      45      2 mins ago    │         │
│  │ Fyber        -       -      Never         │         │
│  │ InMobi       8      23      1 hour ago    │         │
│  └────────────────────────────────────────────┘         │
│                                                           │
│  ⚙️ Settings                                              │
│  [Manage Network Credentials] [Export All Data]          │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### 2.2 Create Media/App 페이지 (네트워크별 동적 렌더링)
```
┌──────────────────────────────────────────────────────────┐
│  📱 Create New App                                       │
│  Network: [BigOAds ▼]  (선택된 네트워크에 따라 UI 변경) │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  ┌─────────────────────────────────────────────┐        │
│  │ 현재 선택: BigOAds                           │        │
│  │ Support: ✅ Create API Available             │        │
│  └─────────────────────────────────────────────┘        │
│                                                           │
│  ═══════════════════════════════════════════════         │
│  COMMON FIELDS (대부분의 네트워크 공통)                   │
│  ═══════════════════════════════════════════════         │
│  ┌────────────────────────────────────────────────┐     │
│  │ App Name*:        [_________________________]  │     │
│  │ Package Name*:    [_________________________]  │     │
│  │ Platform*:        [Radio] Android  [ ] iOS     │     │
│  │ Store URL:        [_________________________]  │     │
│  └────────────────────────────────────────────────┘     │
│                                                           │
│  ═══════════════════════════════════════════════         │
│  NETWORK-SPECIFIC FIELDS                                 │
│  ═══════════════════════════════════════════════         │
│                                                           │
│  ┌────── IF BigOAds ──────────────────────────────┐     │
│  │ Media Type*:       [Radio] Application [ ] Site│     │
│  │ iTunes ID (iOS):   [_________________________] │     │
│  │                                                 │     │
│  │ Category*:         [Dropdown ▼]                │     │
│  │                    - Action, Adventure, etc.   │     │
│  │                                                 │     │
│  │ Mediation Platform*: [Multi-select]            │     │
│  │  ☑ MAX  ☑ AdMob  ☑ ironSource  ☐ TopOn       │     │
│  │  ☐ Yandex  ☐ TradePlus  ☐ In-house           │     │
│  │  ☐ Others: [_________________]                 │     │
│  │                                                 │     │
│  │ COPPA*:            [Radio] No  [ ] Yes         │     │
│  │ Orientation*:      [Radio] Vertical  [ ] Horiz │     │
│  └────────────────────────────────────────────────┘     │
│                                                           │
│  ┌────── IF Fyber (예시) ──────────────────────────┐    │
│  │ App Bundle ID*:    [_________________________]  │    │
│  │ App Store ID:      [_________________________]  │    │
│  │ Country:           [Dropdown ▼]                 │    │
│  │ Category:          [Dropdown ▼]                 │    │
│  └────────────────────────────────────────────────┘     │
│                                                           │
│  ┌────── IF InMobi (예시) ──────────────────────────┐   │
│  │ App Type*:         [Radio] App  [ ] Mobile Web  │   │
│  │ Genre:             [Dropdown ▼]                  │   │
│  │ Content Rating:    [Dropdown ▼]                  │   │
│  └────────────────────────────────────────────────┘     │
│                                                           │
│  ⚠️ Note: Different networks require different fields    │
│                                                           │
│  ┌──────────────┐  ┌──────────────┐                     │
│  │   Reset      │  │  Create App  │                     │
│  └──────────────┘  └──────────────┘                     │
│                                                           │
│  📝 Result                                                │
│  ┌────────────────────────────────────────────────┐     │
│  │ [Success/Error message will appear here]       │     │
│  │                                                 │     │
│  │ Network: BigOAds                               │     │
│  │ Created App Code: 10*****7                     │     │
│  │ App Name: MyTestApp                            │     │
│  └────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────┘
```

**네트워크별 필드 매핑 전략:**
- `network_configs/` 폴더에 각 네트워크별 설정 파일
- JSON/YAML로 필드 정의 및 검증 규칙 관리
- 동적 UI 렌더링 엔진 사용

### 2.3 Create Slot 페이지
```
┌──────────────────────────────────────────────────────────┐
│  🎯 Create New Slot                                      │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  Select App                                               │
│  ┌────────────────────────────────────────────────┐     │
│  │ App Code*:  [Dropdown ▼ or Text Input]         │     │
│  │             - 10****7 (MyTestApp)              │     │
│  │             - 10****8 (AnotherApp)             │     │
│  └────────────────────────────────────────────────┘     │
│                                                           │
│  Basic Information                                        │
│  ┌────────────────────────────────────────────────┐     │
│  │ Slot Name*:     [_________________________]    │     │
│  │                                                 │     │
│  │ Ad Type*:       [Dropdown ▼]                   │     │
│  │                 - Native                        │     │
│  │                 - Banner                        │     │
│  │                 - Interstitial                  │     │
│  │                 - Reward Video                  │     │
│  │                 - Splash Ad                     │     │
│  │                 - Pop Up                        │     │
│  │                                                 │     │
│  │ Auction Type*:  [Dropdown ▼]                   │     │
│  │                 - Waterfall                     │     │
│  │                 - Client Bidding                │     │
│  │                 - Server Bidding                │     │
│  └────────────────────────────────────────────────┘     │
│                                                           │
│  🔽 Advanced Settings (Conditional - based on Ad Type)   │
│  ┌────────────────────────────────────────────────┐     │
│  │ [If Waterfall]                                  │     │
│  │ Reserve Price:  [$__.__] (cents, default 0.01) │     │
│  │                                                 │     │
│  │ [If Native/Interstitial/Reward/PopUp]          │     │
│  │ Music:          [Radio] On  [ ] Off            │     │
│  │                                                 │     │
│  │ [If Native]                                     │     │
│  │ Creative Type*: ☑ Image  ☑ Video              │     │
│  │ Video Replay:   [Radio] Yes  [ ] No            │     │
│  │                                                 │     │
│  │ [If Banner]                                     │     │
│  │ Auto Refresh*:  [Radio] Yes  [ ] No            │     │
│  │ Refresh Sec:    [_____] seconds                │     │
│  │ Banner Size*:   ☑ 300x250  ☑ 320x50           │     │
│  │                                                 │     │
│  │ [If Splash]                                     │     │
│  │ Full Screen*:   [Radio] Full  [ ] Half         │     │
│  │ Show Duration*: [___] (3-10s)                  │     │
│  │ Turn Off*:      [___] (0-5s)                   │     │
│  │ Show Count Max*:[___] (0=free)                 │     │
│  │ Interactive*:   [Radio] No  [ ] Yes            │     │
│  └────────────────────────────────────────────────┘     │
│                                                           │
│  ┌──────────────┐  ┌──────────────┐                     │
│  │   Reset      │  │  Create Slot │                     │
│  └──────────────┘  └──────────────┘                     │
│                                                           │
│  📝 Result                                                │
│  ┌────────────────────────────────────────────────┐     │
│  │ [Success/Error message]                         │     │
│  │                                                 │     │
│  │ Created Slot Code: 12345-67890                 │     │
│  │ Slot Name: MyTestSlot                          │     │
│  └────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────┘
```

### 2.4 View Lists 페이지
```
┌──────────────────────────────────────────────────────────┐
│  📋 View Apps & Slots                                    │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  🔍 Filters                                               │
│  ┌────────────────────────────────────────────────┐     │
│  │ View: [Radio] Apps  [ ] Slots                  │     │
│  │                                                 │     │
│  │ [If Slots selected]                            │     │
│  │ App Code: [Dropdown ▼ - Select app first]     │     │
│  │                                                 │     │
│  │ ┌──────────────┐                               │     │
│  │ │   Refresh    │                               │     │
│  │ └──────────────┘                               │     │
│  └────────────────────────────────────────────────┘     │
│                                                           │
│  📊 Apps List                                             │
│  ┌────────────────────────────────────────────────┐     │
│  │ ┌────────┬──────────┬─────────┬──────────┐    │     │
│  │ │AppCode │Name      │Platform │Status    │    │     │
│  │ ├────────┼──────────┼─────────┼──────────┤    │     │
│  │ │10****7 │MyTestApp │Android  │Active    │    │     │
│  │ │10****8 │TestApp2  │iOS      │Active    │    │     │
│  │ └────────┴──────────┴─────────┴──────────┘    │     │
│  │                                                 │     │
│  │ Total: 2 apps                                  │     │
│  │ Page: [1] [2] [3] ... [Next]                  │     │
│  └────────────────────────────────────────────────┘     │
│                                                           │
│  OR                                                       │
│                                                           │
│  📊 Slots List (for selected app)                        │
│  ┌────────────────────────────────────────────────┐     │
│  │ App: MyTestApp (10****7)                       │     │
│  │                                                 │     │
│  │ ┌──────────┬──────────┬───────┬────────┐      │     │
│  │ │SlotCode  │Name      │AdType │Auction │      │     │
│  │ ├──────────┼──────────┼───────┼────────┤      │     │
│  │ │12345-678 │TestSlot1 │Native │Waterf. │      │     │
│  │ │12345-679 │TestSlot2 │Banner │Client  │      │     │
│  │ └──────────┴──────────┴───────┴────────┘      │     │
│  │                                                 │     │
│  │ Total: 2 slots                                 │     │
│  └────────────────────────────────────────────────┘     │
│                                                           │
│  💾 Export                                                │
│  ┌────────────┐  ┌────────────┐                         │
│  │ Export CSV │  │ Export JSON│                         │
│  └────────────┘  └────────────┘                         │
└──────────────────────────────────────────────────────────┘
```

## 3. 파일 구조 (확장 가능한 멀티 네트워크 아키텍처)

```
ad_network_streamlit/
├── app.py                           # 메인 앱 (Home - Network Hub)
│
├── pages/
│   ├── 1_📱_Create_App.py          # Create App (네트워크별 동적)
│   ├── 2_🎯_Create_Unit.py         # Create Unit (네트워크별 동적)
│   └── 3_📋_View_Lists.py          # View Lists (멀티 네트워크)
│
├── utils/
│   ├── __init__.py
│   ├── network_manager.py          # AdNetworkManager 래퍼
│   ├── session_manager.py          # Streamlit 세션 상태 관리
│   ├── validators.py               # 공통 검증 함수들
│   └── ui_components.py            # 재사용 가능한 UI 컴포넌트
│
├── network_configs/                # 네트워크별 설정 (핵심!)
│   ├── __init__.py
│   ├── base_config.py              # 기본 인터페이스
│   ├── bigoads_config.py           # BigOAds 설정
│   ├── fyber_config.py             # Fyber 설정
│   ├── inmobi_config.py            # InMobi 설정
│   ├── ironsource_config.py        # IronSource 설정
│   ├── mintegral_config.py         # Mintegral 설정
│   ├── pangle_config.py            # Pangle 설정
│   └── liftoff_config.py           # Liftoff 설정
│
├── network_schemas/                # 네트워크별 스키마 정의 (옵션)
│   ├── bigoads_schema.json
│   ├── fyber_schema.json
│   └── ...
│
├── BE/                             # 기존 백엔드 (참조)
│   ├── models/
│   │   └── source.py
│   └── services/
│       ├── ad_network_api.py       # API 클라이언트들
│       └── ad_network_manager.py   # 통합 매니저
│
├── .env                            # 환경 변수 (모든 네트워크)
├── requirements.txt
└── README.md
```

### 핵심 설계 원칙

#### 1. **네트워크 Config 패턴** (Strategy Pattern)
각 네트워크는 독립적인 config 클래스로 정의:

```python
# network_configs/base_config.py
class NetworkConfig(ABC):
    """모든 네트워크 설정의 기본 인터페이스"""
    
    @property
    @abstractmethod
    def network_name(self) -> str:
        pass
    
    @abstractmethod
    def get_app_creation_fields(self) -> List[Field]:
        """앱 생성 필드 정의"""
        pass
    
    @abstractmethod
    def get_unit_creation_fields(self, ad_type: str = None) -> List[Field]:
        """유닛 생성 필드 정의 (ad_type에 따라 동적)"""
        pass
    
    @abstractmethod
    def validate_app_data(self, data: Dict) -> Tuple[bool, str]:
        """앱 생성 데이터 검증"""
        pass
    
    @abstractmethod
    def build_app_payload(self, form_data: Dict) -> Dict:
        """API 요청 payload 생성"""
        pass

# network_configs/bigoads_config.py
class BigOAdsConfig(NetworkConfig):
    network_name = "BigOAds"
    
    def get_app_creation_fields(self):
        return [
            Field("name", "text", required=True, label="App Name"),
            Field("mediaType", "radio", required=True, 
                  options=[("Application", 1), ("Site", 2)]),
            Field("platform", "radio", required=True,
                  options=[("Android", 1), ("iOS", 2)]),
            Field("pkgName", "text", required=True, label="Package Name"),
            Field("category", "dropdown", required=True,
                  options=self._get_categories()),
            Field("mediationPlatform", "multiselect", required=True,
                  options=self._get_mediation_platforms()),
            Field("coppaOption", "radio", required=True,
                  options=[("No", 1), ("Yes", 2)]),
            Field("screenDirection", "radio", required=True,
                  options=[("Vertical", 0), ("Horizontal", 1)]),
            # iOS 전용 (조건부)
            ConditionalField("itunesId", "number", 
                           condition=lambda data: data.get("platform") == 2,
                           required=True),
        ]
    
    def get_unit_creation_fields(self, ad_type: str = None):
        base_fields = [
            Field("appCode", "dropdown", required=True),
            Field("name", "text", required=True, label="Slot Name"),
            Field("adType", "dropdown", required=True,
                  options=self._get_ad_types()),
            Field("auctionType", "dropdown", required=True,
                  options=self._get_auction_types()),
        ]
        
        # ad_type에 따른 조건부 필드
        if ad_type == "native":
            base_fields.extend([
                Field("musicSwitch", "radio", required=True),
                Field("adSpecification", "multiselect", required=True),
                ConditionalField("videoAutoReplay", "radio",
                               condition=lambda d: "video" in d.get("adSpecification", [])),
            ])
        # ... 기타 ad_type별 필드
        
        return base_fields
```

#### 2. **동적 UI 렌더러**
```python
# utils/ui_components.py
class DynamicFormRenderer:
    """네트워크 Config 기반 동적 폼 렌더링"""
    
    @staticmethod
    def render_field(field: Field, form_data: Dict) -> Any:
        """필드 타입에 따라 적절한 Streamlit 위젯 렌더링"""
        if field.field_type == "text":
            return st.text_input(field.label, key=field.name)
        elif field.field_type == "radio":
            return st.radio(field.label, 
                          options=[opt[0] for opt in field.options],
                          key=field.name)
        elif field.field_type == "dropdown":
            return st.selectbox(field.label, field.options, key=field.name)
        # ... 기타 필드 타입
    
    @staticmethod
    def render_form(config: NetworkConfig, form_type: str) -> Dict:
        """전체 폼 렌더링"""
        if form_type == "app":
            fields = config.get_app_creation_fields()
        elif form_type == "unit":
            fields = config.get_unit_creation_fields()
        
        form_data = {}
        for field in fields:
            # 조건부 필드는 조건 확인
            if isinstance(field, ConditionalField):
                if not field.condition(form_data):
                    continue
            
            form_data[field.name] = DynamicFormRenderer.render_field(field, form_data)
        
        return form_data
```

#### 3. **Network Registry**
```python
# network_configs/__init__.py
NETWORK_REGISTRY = {
    'bigoads': BigOAdsConfig(),
    'fyber': FyberConfig(),
    'inmobi': InMobiConfig(),
    'ironsource': IronSourceConfig(),
    'mintegral': MintegralConfig(),
    'pangle': PangleConfig(),
    'liftoff': LiftoffConfig(),
}

def get_network_config(network_name: str) -> NetworkConfig:
    """네트워크 설정 객체 반환"""
    return NETWORK_REGISTRY.get(network_name.lower())

def get_available_networks() -> List[str]:
    """사용 가능한 네트워크 목록"""
    return list(NETWORK_REGISTRY.keys())
```

## 4. 주요 기능 흐름

### 4.1 Create Media 흐름
```
1. 사용자 입력 받기
   ↓
2. 입력 검증 (validators.py)
   ↓
3. API 요청 payload 생성
   ↓
4. BigOAdsAPI.create_media() 호출
   ↓
5. 결과 표시 (success/error)
   ↓
6. 성공 시 appCode 저장 (세션 상태)
```

### 4.2 Create Slot 흐름
```
1. 앱 목록 로드 (get_apps)
   ↓
2. 앱 선택 & Slot 정보 입력
   ↓
3. Ad Type에 따라 동적 필드 표시
   ↓
4. 입력 검증
   ↓
5. API 요청 payload 생성
   ↓
6. BigOAdsAPI.create_slot() 호출
   ↓
7. 결과 표시
```

### 4.3 View Lists 흐름
```
1. Apps/Slots 선택
   ↓
2. API 호출
   - Apps: get_apps()
   - Slots: get_placements(app_code)
   ↓
3. 데이터 테이블 표시 (st.dataframe)
   ↓
4. 페이지네이션 (필요시)
   ↓
5. Export 기능 (CSV/JSON)
```

## 4. 데이터 플로우 (멀티 네트워크)

```
┌─────────────────────────────────────────────────────────┐
│ User selects Network (e.g., BigOAds)                    │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│ NetworkRegistry.get_config('bigoads')                   │
│ → Returns BigOAdsConfig instance                        │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│ DynamicFormRenderer.render_form(config, 'app')         │
│ → Generates form based on config.get_app_fields()      │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│ User fills form                                         │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│ Validation: config.validate_app_data(form_data)        │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│ Build Payload: config.build_app_payload(form_data)     │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│ Get API Client: AdNetworkManager.get_client('bigoads') │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│ API Call: client.create_app(payload)                   │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│ Handle Response & Display Result                        │
└─────────────────────────────────────────────────────────┘
```

### 장점:
1. **확장성**: 새 네트워크 추가 시 config 파일만 추가
2. **유지보수**: 각 네트워크 로직이 독립적
3. **재사용성**: 공통 UI 컴포넌트와 검증 로직 공유
4. **테스트**: 네트워크별 독립 테스트 가능

## 5. 세션 상태 관리 (멀티 네트워크)

```python
st.session_state = {
    # 네트워크 관리
    'selected_network': 'bigoads',           # 현재 선택된 네트워크
    'network_manager': AdNetworkManager,     # 통합 매니저 인스턴스
    'initialized_networks': ['bigoads'],     # 초기화 성공한 네트워크들
    
    # 네트워크별 데이터 캐시 (구조: {network: {data}})
    'apps_cache': {
        'bigoads': [],
        'fyber': [],
        # ...
    },
    'units_cache': {
        'bigoads': {},  # {app_id: [units]}
        'fyber': {},
        # ...
    },
    
    # 생성 히스토리
    'created_apps': [
        {'network': 'bigoads', 'app_code': '10****7', 'name': 'Test', 'timestamp': '...'},
        # ...
    ],
    'created_units': [
        {'network': 'bigoads', 'slot_code': '12345-67890', 'name': 'TestSlot', 'timestamp': '...'},
        # ...
    ],
    
    # UI 상태
    'last_sync_time': {
        'bigoads': datetime.now(),
        'fyber': None,
        # ...
    },
    'error_log': [],  # 에러 히스토리
}
```

### 세션 관리 유틸리티:
```python
# utils/session_manager.py
class SessionManager:
    @staticmethod
    def switch_network(network_name: str):
        """네트워크 전환"""
        st.session_state.selected_network = network_name
        
    @staticmethod
    def get_current_network() -> str:
        """현재 선택된 네트워크"""
        return st.session_state.get('selected_network', 'bigoads')
    
    @staticmethod
    def cache_apps(network: str, apps: List[Dict]):
        """앱 목록 캐시"""
        if 'apps_cache' not in st.session_state:
            st.session_state.apps_cache = {}
        st.session_state.apps_cache[network] = apps
        st.session_state.last_sync_time[network] = datetime.now()
    
    @staticmethod
    def get_cached_apps(network: str) -> List[Dict]:
        """캐시된 앱 목록 가져오기"""
        return st.session_state.get('apps_cache', {}).get(network, [])
    
    @staticmethod
    def add_created_app(network: str, app_data: Dict):
        """생성된 앱 히스토리에 추가"""
        if 'created_apps' not in st.session_state:
            st.session_state.created_apps = []
        st.session_state.created_apps.append({
            'network': network,
            'timestamp': datetime.now().isoformat(),
            **app_data
        })
```

## 6. 구현 우선순위 (단계적 확장 전략)

### Phase 1: 코어 아키텍처 + BigOAds (2-3일)
**목표**: 확장 가능한 기반 구조 완성 + BigOAds 완전 구현

1. ✅ **아키텍처 설계**
   - NetworkConfig 추상 클래스
   - NetworkRegistry 패턴
   - DynamicFormRenderer

2. ✅ **BigOAds 완전 구현**
   - `bigoads_config.py` (필드 정의, 검증, payload 빌드)
   - API Client에 create_media, create_slot 메서드 추가
   - Streamlit 페이지 구현 (Home, Create App, Create Unit, View)

3. ✅ **공통 유틸리티**
   - SessionManager
   - 기본 UI 컴포넌트
   - 공통 Validators

**결과물**: BigOAds 완전 동작 + 다른 네트워크 추가 준비 완료

---

### Phase 2: 멀티 네트워크 확장 - Tier 1 (3-4일)
**목표**: Fyber, InMobi, IronSource 추가

4. 🔄 **각 네트워크별 Config 작성**
   - API 문서 조사 (Create App/Unit 엔드포인트)
   - `{network}_config.py` 작성
   - API Client에 create 메서드 추가

5. 🔄 **테스트 & 검증**
   - 각 네트워크별 실제 API 테스트
   - 에러 핸들링 강화

**결과물**: 4개 네트워크 완전 지원

---

### Phase 3: 멀티 네트워크 확장 - Tier 2 (2-3일)
**목표**: Mintegral, Pangle, Liftoff 추가

6. 🔄 **나머지 네트워크 Config**
7. 🔄 **통합 테스트**

**결과물**: 7개 네트워크 완전 지원

---

### Phase 4: 고급 기능 (선택, 2-3일)
8. 🔄 **대시보드 강화**
   - 멀티 네트워크 통계
   - 비교 차트 (네트워크별 앱/유닛 수)

9. 🔄 **배치 작업**
   - 여러 네트워크에 동시 앱 생성
   - 템플릿 기반 생성

10. 🔄 **데이터 동기화**
    - 자동 새로고침
    - 변경 감지

11. 🔄 **Export/Import**
    - 설정 내보내기/가져오기
    - 전체 데이터 백업

---

### Phase 5: 최적화 & 프로덕션 준비 (2-3일)
12. 🔄 **성능 최적화**
    - 캐싱 전략 개선
    - API 호출 최소화

13. 🔄 **에러 처리 & 로깅**
    - 상세한 에러 메시지
    - 로그 시스템

14. 🔄 **문서화**
    - 사용자 가이드
    - 개발자 문서 (새 네트워크 추가 방법)

15. 🔄 **보안**
    - Credential 안전한 관리
    - API 키 암호화

## 8. 핵심 컴포넌트

### 8.1 동적 필드 렌더링
```python
def render_conditional_fields(ad_type: str, auction_type: str):
    """Ad Type과 Auction Type에 따라 필요한 필드만 표시"""
    if auction_type == "waterfall":
        st.number_input("Reserve Price (cents)", min_value=1, value=1)
    
    if ad_type in ["native", "interstitial", "reward_video", "pop_up"]:
        st.radio("Music Switch", ["Sound On", "Sound Off"])
    
    if ad_type == "native":
        st.multiselect("Creative Type", ["Image", "Video"])
        st.radio("Video Auto Replay", ["Yes", "No"])
    
    # ... 기타 조건부 필드
```

### 8.2 API 응답 처리
```python
def handle_api_response(response: Dict):
    """API 응답 처리 및 표시"""
    if response.get('status') == 0:
        st.success("✅ Success!")
        result = response.get('result', {})
        st.json(result)
        return result
    else:
        st.error(f"❌ Error: {response.get('code')} - {response.get('msg')}")
        return None
```

## 9. 확장성 가이드: 새 네트워크 추가하기

### Step-by-Step 프로세스

#### 1. API 문서 조사
```
필요한 정보:
- 인증 방식
- Create App 엔드포인트 (URL, Method, Headers, Payload)
- Create Unit 엔드포인트
- 필수/선택 필드 목록
- 필드 타입 및 제약사항
- 응답 형식
```

#### 2. Config 클래스 작성
```python
# network_configs/newnetwork_config.py
from .base_config import NetworkConfig, Field, ConditionalField

class NewNetworkConfig(NetworkConfig):
    network_name = "NewNetwork"
    display_name = "New Network"
    
    def get_app_creation_fields(self) -> List[Field]:
        return [
            Field("app_name", "text", required=True, 
                  label="App Name",
                  help="Enter your app name"),
            Field("platform", "radio", required=True,
                  options=[("Android", "android"), ("iOS", "ios")]),
            # ... 더 많은 필드
        ]
    
    def validate_app_data(self, data: Dict) -> Tuple[bool, str]:
        # 커스텀 검증 로직
        if not data.get("app_name"):
            return False, "App name is required"
        return True, ""
    
    def build_app_payload(self, form_data: Dict) -> Dict:
        # API 요구사항에 맞게 payload 변환
        return {
            "name": form_data["app_name"],
            "platform": form_data["platform"],
            # ...
        }
    
    def get_unit_creation_fields(self, ad_type: str = None) -> List[Field]:
        # 유닛 생성 필드
        pass
```

#### 3. API Client 확장
```python
# BE/services/ad_network_api.py

class NewNetworkAPI:
    def __init__(self, api_key: str):
        self.api_key = api_key
        self.base_url = "https://api.newnetwork.com"
    
    def create_app(self, payload: Dict) -> Dict:
        """앱 생성 API"""
        url = f"{self.base_url}/apps"
        response = requests.post(url, json=payload, 
                               headers=self._get_headers())
        response.raise_for_status()
        return response.json()
    
    def create_unit(self, payload: Dict) -> Dict:
        """유닛 생성 API"""
        pass
    
    def get_apps(self) -> Dict:
        """앱 목록 조회"""
        pass
```

#### 4. Registry에 등록
```python
# network_configs/__init__.py
from .newnetwork_config import NewNetworkConfig

NETWORK_REGISTRY = {
    'bigoads': BigOAdsConfig(),
    'fyber': FyberConfig(),
    # ...
    'newnetwork': NewNetworkConfig(),  # 추가!
}
```

#### 5. Manager에 초기화 로직 추가
```python
# BE/services/ad_network_manager.py
def _initialize_clients(self):
    # ...
    
    # NewNetwork
    if 'newnetwork_api_key' in creds and creds['newnetwork_api_key']:
        try:
            self.clients['newnetwork'] = NewNetworkAPI(
                creds['newnetwork_api_key']
            )
        except Exception as e:
            print(f"Failed to initialize NewNetwork: {e}")
```

#### 6. 환경 변수 추가
```bash
# .env
NEWNETWORK_API_KEY=your_api_key_here
```

#### 7. 테스트
```python
# test_newnetwork.py
def test_newnetwork_api():
    api = NewNetworkAPI(os.getenv('NEWNETWORK_API_KEY'))
    
    # 앱 생성 테스트
    payload = {
        "name": "Test App",
        "platform": "android"
    }
    result = api.create_app(payload)
    assert result['success'] == True
```

### 완료! 🎉
새 네트워크가 자동으로 UI에 나타나고 모든 기능 사용 가능

---

## 10. 네트워크별 상태 추적

### Support Matrix 유지
```python
# network_configs/support_matrix.py
NETWORK_SUPPORT = {
    'bigoads': {
        'create_app': True,
        'create_unit': True,
        'get_apps': True,
        'get_units': True,
        'update_app': False,
        'delete_app': False,
    },
    'fyber': {
        'create_app': False,  # API 없음
        'create_unit': False,
        'get_apps': True,
        'get_units': True,
        'update_app': False,
        'delete_app': False,
    },
    # ...
}

def is_feature_supported(network: str, feature: str) -> bool:
    """특정 네트워크가 기능을 지원하는지 확인"""
    return NETWORK_SUPPORT.get(network, {}).get(feature, False)
```

### UI에서 사용
```python
# pages/1_Create_App.py
network = SessionManager.get_current_network()

if not is_feature_supported(network, 'create_app'):
    st.warning(f"⚠️ {network} does not support app creation via API")
    st.info("Please create apps manually in the network's dashboard")
    st.stop()

# 지원하는 경우에만 폼 표시
config = get_network_config(network)
form_data = DynamicFormRenderer.render_form(config, 'app')
```

---

## 11. 사용자 경험 개선사항

1. **입력 검증**: 실시간 검증 및 피드백
2. **자동완성**: Package Name 입력 시 Store URL 자동 생성
3. **도움말**: 각 필드 옆에 `st.help()` 아이콘
4. **미리보기**: 생성 전 payload 미리보기
5. **히스토리**: 최근 생성한 앱/슬롯 목록
6. **검색**: 앱 목록에서 검색 기능
7. **네트워크 전환**: 사이드바에서 빠른 전환
8. **배치 작업**: 여러 네트워크에 동시 생성

---

## 12. 다음 단계 및 확정

### 확정해야 할 사항:
1. ✅ **Phase 1부터 시작** (코어 아키텍처 + BigOAds)?
2. ✅ **파일 구조** 위 제안대로?
3. ✅ **NetworkConfig 패턴** 사용?

### 즉시 시작 가능한 작업:

#### A. 기본 구조 생성
```bash
mkdir -p ad_network_streamlit/{pages,utils,network_configs}
touch ad_network_streamlit/app.py
touch ad_network_streamlit/pages/{1_Create_App.py,2_Create_Unit.py,3_View_Lists.py}
```

#### B. 순서:
1. **base_config.py** 작성 (추상 클래스)
2. **bigoads_config.py** 작성 (BigOAds 구체 구현)
3. **ad_network_api.py** 확장 (create_media, create_slot)
4. **app.py** 작성 (Home - Network Hub)
5. **1_Create_App.py** 작성
6. **2_Create_Unit.py** 작성
7. **3_View_Lists.py** 작성
8. 테스트 & 디버깅

이 방향으로 진행하시겠습니까? 
아니면 특정 부분을 먼저 상세히 설계하거나 수정하시겠습니까?
