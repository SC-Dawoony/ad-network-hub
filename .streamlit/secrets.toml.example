# Streamlit Secrets 예시
# 이 파일을 secrets.toml로 복사 후 실제 값을 채워주세요.
# 로컬: .streamlit/secrets.toml (로컬에서는 인증 비활성화, 이 파일 불필요)
# Streamlit Cloud: 앱 설정에서 Secrets 추가

# ===========================================
# Auth (프로덕션에서만 필요)
# ===========================================
# 로컬: 설정 안 함 → 인증 비활성화
# 프로덕션: "true" → Google OAuth 로그인 필수
# ENABLE_AUTH = "true"

# ===========================================
# Google OAuth (App Login) - 프로덕션에서만
# ===========================================
GOOGLE_CLIENT_ID = "your_google_client_id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET = "your_google_client_secret"
GOOGLE_REDIRECT_URI = "http://localhost:8501/login"

# 배포 시 Redirect URI 예시 (0_Login.py → /login 경로):
# GOOGLE_REDIRECT_URI = "https://your-app-name.streamlit.app/login"
